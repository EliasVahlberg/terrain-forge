[package]
name = "terrain-forge-demo"
version = "0.1.0"
edition = "2021"

[dependencies]
terrain-forge = { path = ".." }
clap = { version = "4.0", features = ["derive"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
image = "0.24"
